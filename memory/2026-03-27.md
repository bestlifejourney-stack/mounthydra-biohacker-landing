# 2026-03-27

## Key Principle (from Todd)
**Never put a human in the path of execution.** If there's an automated path, use it. Manual data entry is a non-starter. This applies to everything — lead capture, follow-up, data transfer. Automate or don't ship it.

## Conference Landing Page — Final Day
- 3-pack price set to $1 for Todd's end-to-end test, then restored to $1,215
- QR code generated for conference.mounthydra.us
- Checkout pre-fill: billing data carries from landing page → WooCommerce checkout
- Lead capture: every form submission emails info@mounthydra.com via WP REST endpoint
- Cart icon fix + checkout pre-fill + lead capture all added to functions.php via AppleScript
- WordPress password reset successful (ClarkWP2026!mhUs) but Members plugin still blocks wp-admin
- Workaround: use Todd's Chrome session via AppleScript for wp-admin changes

## GHL Integration — Blocked
- GHL API key from Kato's handover is expired/invalid
- GHL SPA won't render through AppleScript automation (stuck on "Initializing...")
- Need to find automated path to GHL — possibly via Zapier (plugin already installed on WP)
- TODO: Set up Zapier workflow to push leads from WP to GHL automatically
